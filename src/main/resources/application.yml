spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fan_of_holiday?characterEncoding=utf8&autoReconnect=true
    username: holiday
    password: 9595
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql   # 추가 해준 부분
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect # 추가 해준 부분

    hibernate:
      use-new-id-generator-mappings: false
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

